# API 베이스 URL. MSW 를 쓰는 동안에는 상대경로 /api 로 두면 된다.
VITE_API_BASE_URL=/api

# 'true' 이면 MSW 목업 서버를 켠다. 실제 백엔드에 붙일 때 'false' 로 바꾼다.
VITE_ENABLE_MSW=true

# 목업 응답에 넣을 인위적 지연(ms). 로딩 UI 확인용.
VITE_MOCK_LATENCY=300
